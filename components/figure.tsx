import React from "react";

type Props = {};

const Figure = (props: Props) => {
  return (
    <div className="flex lg:w-[50%] w-full items-center justify-center md:items-start">
      Figure
    </div>
  );
};

export default Figure;
